import { types } from '../types/types';

const state = {
    
}

export const ReducerMovies = (state = {}, action) => {
    switch (action.type) {
        case types.save:
            
            break;

        case types.delete:
        
        break;
    
        default:
            break;
    }
}
